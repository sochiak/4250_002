ChickenContenders
•	Risk / Technology
1.	Admin: AD technology
a)	What if account gets locked out 
b)	What happens if someone tries to login as admin 
2.	Admin creating new hunt:
a)	GPS coordinates –
b)	Security risk of location-based date – 
c)	Tech: Map software, location tracker, group text system, URL generator

3.	Admin Accounts:
a)	Risk: data leak of user information

4.	If multiple tasks are in perimeter, then then it can be mitigated by having the user fill out what specific task they’re doing at the moment. 
